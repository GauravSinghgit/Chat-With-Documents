NEXT_PUBLIC_API_URL=http://localhost:8000

# Must match the backend's JWT_SECRET exactly — middleware.ts verifies the
# access_token cookie's signature locally (no round-trip to the backend).
JWT_SECRET=
